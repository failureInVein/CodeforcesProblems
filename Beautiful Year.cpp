#include <iostream>
using namespace std;
int main()
{
    int i;
    long long n;
    cin >> n;
    int a[i];
    for(i=n;i<=9000;i++)
    {
        if(a[i]!=a[i+1]!=a[i+2]!=a[i+3] && a[i]>n)
    }
    cout << a[i];
}
